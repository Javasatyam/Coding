package login_Ui;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Action;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import io.github.bonigarcia.wdm.WebDriverManager;

public class HoverOnButton {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		WebDriverManager.chromedriver().setup();
		WebDriver driver=new ChromeDriver();
//        driver.get("https://www.amazon.in");
//      WebElement ele=driver.findElement(By.xpath("//*[@id=\"nav-link-accountList-nav-line-1\"]"));
//      Actions s=new Actions(driver);
//      s.moveToElement(ele).perform();
//      s.doubleClick(ele).perform();
//		FileInputStream s=new FileInputStream("");
		//insert into emp(id,name,loc)values('1','Satyam','xyz');
		//
//		WebElement ele=driver.findElement(By.xpath("\"//*[@id=\\\"nav-link-accountList-nav-line-1\\"));
//	     Actions s=new Actions(driver);
//	     s.moveToElement(ele).perform();
//	     WebDriverWait wait = new WebDriverWait(driver, 10); // Specify the timeout duration in seconds
	     
		
 
    

 }
}
